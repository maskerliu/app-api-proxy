name: CI
on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: macOS-latest
  
  steps:
  - uses: actions/checkout@v2

  - name: install dependencies
    run: pnpm install

  - name: build
    run: pnpm run build